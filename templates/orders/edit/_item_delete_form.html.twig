<form method="post" action="{{ path('app_order_items_delete', {'id': orderItem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orderItem.id) }}">
    <button class="btn">Delete</button>
</form>