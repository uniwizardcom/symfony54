{% extends 'base.html.twig' %}

{% block title %}New Orders{% endblock %}

{% block body %}
<div class="example-wrapper container">
    <h1>Create new Orders</h1>

    <a href="{{ path('app_index') }}">Home</a>

    {{ include('orders/_form.html.twig') }}

    <a href="{{ path('app_orders_index') }}">back to list</a>
</div>
{% endblock %}
